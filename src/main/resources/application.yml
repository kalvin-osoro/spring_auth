spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt_security?useSSL=false&serverTimeZone=UTC*useLegacyDateTimeCode=false
    username: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
#    database-platform: org.hibernate.dialect.MySQLInnoDBDialect

